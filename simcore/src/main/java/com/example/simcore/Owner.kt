package com.example.simcore

/**
 * Owner identifiers.
 * Neutral is not a "player entity" in the full spec, but for now it’s an owner id.
 */
object Owner {
    const val NEUTRAL: Int = 0
    const val P1: Int = 1
    const val P2: Int = 2
    const val P3: Int = 3
    const val P4: Int = 4
}
